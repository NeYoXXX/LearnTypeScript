// 与文件相关的选项
{
    "files": [
        "src/a.ts"
    ], // 编译器需要编译的单个文件的列表
    "include": [
        "src", // src目录下的所有文件，包括子目录下的文件
        "src/*", // 只会编译src目录下的一级目录的文件
        "src/*/*", // 只会编译src目录二级目录文件
    ], // 编译器需要编译的文件或者目录
    "exclude": [

    ], // 需要排除的目录或文件，默认排除node_modules和所有声明文件
}